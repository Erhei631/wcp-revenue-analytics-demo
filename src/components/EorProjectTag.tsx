import { Tag } from 'antd';

/** Matches Billing Dashboard project EOR badge. */
export function EorProjectTag() {
  return (
    <Tag color="purple" className="eor-project-tag" style={{ marginInlineEnd: 0 }}>
      EOR
    </Tag>
  );
}
